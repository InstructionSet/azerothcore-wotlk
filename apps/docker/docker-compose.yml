version: "3.8"
services:
  influxdb:
    image: influxdb:1.8
    container_name: acore-influxdb
    ports:
      - "8086:8086"
    environment:
      INFLUXDB_DB: worldserver
      INFLUXDB_HTTP_AUTH_ENABLED: "false"
    volumes:
      - influxdb-data:/var/lib/influxdb

  grafana:
    image: grafana/grafana-oss:latest
    container_name: acore-grafana
    ports:
      - "3000:3000"
    depends_on:
      - influxdb
    volumes:
      - grafana-data:/var/lib/grafana

volumes:
  influxdb-data:
  grafana-data:
